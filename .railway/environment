# Railway 环境变量配置
# 在 Railway 控制台设置这些环境变量

# 数据库配置（Railway 会自动提供 DATABASE_URL）
# DATABASE_URL=<自动注入>

# 应用配置
HOST=0.0.0.0
PORT=8000
DEBUG=false

# 数据库类型（自动检测）
# Railway 会自动检测 DATABASE_URL 并适配 SQLite/PostgreSQL
